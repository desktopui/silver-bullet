## Nuklear

CLion, GLFW

## Build

On macOS make sure you have `glew` and `glfw`:

```bash
brew install glew
brew install glfw
```

### Development

Open in CLion and run "Debug" configuration or use CLI:

```
cmake
```
